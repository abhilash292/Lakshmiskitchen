spring.application.name=user-service

# ---- Server ----
server.port=8081

# ---- Database connection ----
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/usersdb}
spring.datasource.username=lakshmiskitchen
spring.datasource.password=devpassword

# ---- JPA / Hibernate ----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ---- JWT ----
jwt.secret=${JWT_SECRET:dev-only-secret-do-not-use-in-production-0123456789}

# ---- Actuator ----
management.endpoints.web.exposure.include=health,info,beans,prometheus